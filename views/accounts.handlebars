
<div class="text-center p-3"><h1>Account summary</h1></div>
<div class="text-center p-3"><h2>Welcome Back, {{user.name}}!</h2></div>
    <div>
        <div id="account-container">
                <div class="list-group-item list-group-item-action list-group-item-light p-3">
                    {{#each user.accounts as |account|}} 
                    <div class="account-statement">
                        <a class="activve statement" style="float: right;" href="/statement/{{account.id}}">Statements</a>
                        <h3>Account type: {{account.account_name}} </h3>
                        <p>Balance: £ {{account.balance}} </p>
                    </div>
                    {{/each}}
                </div>
        </div>

    
        
        <div class="p-5">
            <a class="btn btn-outline-primary float-start " onclick="history.back()">Back</a>
        </div>
    </div> 


